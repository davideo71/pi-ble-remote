# Task for Pi-side Claude

## What to do
1. `git pull` to get the latest code
2. Run `python3 pi/ble_receiver.py` for about **2 minutes**
3. Update `pi/REPORT.md` with results (include RSSI), commit and push

## What changed this iteration (Test 35c — range retry)
ESP32 may have been repositioned. Same firmware, same Pi session. Please try scanning again and report RSSI if found.
